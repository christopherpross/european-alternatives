# Logs
logs
# Ignore tmp/ contents, but keep tmp/export/.gitkeep so the directory exists in the repo
/tmp/*
!/tmp/export/
/tmp/export/*
!/tmp/export/.gitkeep
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist/ is gitignored but force-committed by CI (see .github/workflows/deploy.yml).
# NEVER commit dist/ manually — the CI workflow builds and pushes it automatically.
# Hostinger auto-pulls from main and serves from dist/.
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python virtual environments (created by scripts/audit.py)
.venv

# Agent documentation (local only)
agents/
CLAUDE.md
AGENTS.md

# Local runtime secrets for PHP API
api/config/*.local.php
api/config/db.env.php
api/config/admin-token.php

# Research pipeline (contains operational prompts, not for public)
scripts/research-alternative.sh
/scripts/research-batch-issues.sh
